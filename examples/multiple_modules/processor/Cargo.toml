[package]
name = "processor_crate"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
receiver_crate = { path = "../receiver" }
aggregation_crate = { path = "../aggregation" }
