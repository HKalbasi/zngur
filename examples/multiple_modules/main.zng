use "module.zng" as mod_alias;

mod ::std {
    mod vec {
        type Vec<i32> {
            #layout(size = 24, align = 8);
            fn new() -> Vec<i32>;
        }
    }
}

type crate::MainType {
    #layout(size = 8, align = 8);
    fn do_something(&self, mod_alias.::std::option::Option<&i32>);
}
