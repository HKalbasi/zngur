import "packet.zng";

#cpp_additional_includes "
#include <stats.h>
"

type crate::StatsAccumulator {
    #layout(size = 16, align = 8);
    #cpp_value "0" "::cpp_stats::StatsAccumulator";
}

extern "C++" {
    impl crate::StatsAccumulator {
        safe fn create() -> crate::StatsAccumulator;
        safe fn add_packet(&mut self, packet::Packet);
        safe fn print_report(&self);
    }
}
