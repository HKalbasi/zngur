#cpp_additional_includes "
#include <cpp_type.h>
"

type crate::MyCppWrapper {
    #cpp_stack_owned "::CppType" (size = 8, align = 4);
}

extern "C++" {
    impl crate::MyCppWrapper {
        fn constructor(&mut self);
        fn custom_constructor(i32, i32) -> crate::MyCppWrapper;
        fn print(&self);
    }
}
